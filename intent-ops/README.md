# Intent Operations Flow

## Overview

- `instructions.md` is the navigator for AI coding copilot tools to leverage the abilities of the intent-driven workflow.
- Simply extracted `../ai_oriented_kanban/` to `intent-ops/`.
- Extracted folder&file structure `../docs/` to `ops-spec/`.
- `intent-ops` can be copied to any projects and benefit immediately by the standardized intent-driven workflow.

## ops-flow

- follow the folder structure of `../ai_oriented_kanban/` and README.md instructions in each folder.

## ops-spec

- follow the folder structure of `../docs/` and README.md instructions in each folder.
